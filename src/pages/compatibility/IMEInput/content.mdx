import { Card, Typography, Table, Tag, Divider, Alert } from 'antd';
import CodeDiff from '@/components/CodeDiff';
import MermaidViewer from '@/components/MermaidViewer';
import LiveDemo from './LiveDemo';
import badCode from './demos/ime-input.bad.tsx?raw';
import goodCode from './demos/ime-input.good.tsx?raw';
import isComposingPlan from './demos/is-composing-plan.tsx?raw';
import debounceBad from './demos/debounce-bad.tsx?raw';
import isComposingBad from './demos/is-composing-bad.tsx?raw';
import naiveLockBad from './demos/naive-lock-bad.tsx?raw';
import naiveVsPro from './diagrams/naive-vs-pro.mmd?raw';
import imeTiming from './diagrams/ime-timing.mmd?raw';
import { kernelColumns, kernelDataSource, envColumns, envDataSource } from './data';

# 中文输入法 (IME) 组合输入兼容性

<Card title="一、 Bug 出现的现象" style={{ marginBottom: '24px' }}>

当用户使用中文输入法（IME）输入拼音时，浏览器会频繁触发 **input** 事件，导致"拼音碎片"进入业务逻辑。

- **数据污染：** 搜索框会拿着拼音去请求接口，造成无效流量和后端压力。
- **光标跳变：** 在 React 受控模式下，setState 会强制重刷 DOM，导致正在输入的拼音被截断或光标跳到末尾。

</Card>

<Card title="二、 Bug 出现的底层原因" style={{ marginBottom: '24px' }}>

**现代规范的设计：** W3C UI Events 规范对 **compositionend** 和最后一次 **input** 的触发顺序不再做强制规定，因为二者的语义本来就不同：**input** 事件代表 DOM 元素内容发生改变，**compositionend** 则代表**输入法会话（IME Session）的关闭**。当用户按空格或数字键选词上屏时，内容先落地，所以 Chromium / Blink / 现代 WebKit 中通常先触发 **input**，再触发 **compositionend**。

**事件时序不是按内核一刀切：** 它由 Chromium 版本 × 操作系统 IME 管道（TSF / InputMethodKit / IBus / Fcitx）× 输入法框架 × 上屏方式共同决定。因此同样基于 Chromium，Windows 与 Linux、空格上屏与数字选字都可能产生差异。

<Table dataSource={kernelDataSource} columns={kernelColumns} pagination={false} size="small" bordered />

### 不同浏览器内核的事件时序对比

下图清晰地展示了两种主流内核在 IME 输入完成时的事件时序差异：

<MermaidViewer source={imeTiming} />

</Card>

<Card title="三、 Bug 如何解决" style={{ marginBottom: '24px' }}>

现代浏览器优先推荐方案 A：直接监听 **input** 并判断 **!e.nativeEvent.isComposing**，只有在非合成阶段才执行业务逻辑。对于必须兼容 IE 兼容模式或部分老旧国产内核的场景，使用方案 B：**compositionstart** / **compositionend** 标志位锁 + End 中手动补偿触发。

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(450px, 1fr))', gap: '20px' }}>
  <CodeDiff
    oldValue={badCode}
    newValue={goodCode}
    leftTitle="❌ 反面教材"
    rightTitle="✅ 最佳实践"
    type="error"
    hideDiffMarkers={true}
  />
</div>

<CodeDiff
  code={isComposingPlan}
  type="success"
  title="✅ 方案 A：原生 isComposing 属性（现代浏览器首选）"
/>

<CodeDiff
  code={debounceBad}
  type="error"
  title="❌ 误区 A：防抖（Debounce）"
/>

<CodeDiff
  code={isComposingBad}
  type="error"
  title="❌ 误区 B：依赖 isComposing 属性但不兜底"
/>

<CodeDiff
  code={naiveLockBad}
  type="error"
  title="❌ 误区 C：只解锁不补偿"
/>

### 为什么防抖（Debounce）无法解决拼音碎片？

防抖的本质是"停止输入后 N 毫秒才执行"，但它无法区分输入内容是否处于 IME 合成阶段。

- 用户输入长拼音（如 **"zhuang"**）时，中间会有自然停顿思考选字，此时防抖到期触发，携带的仍是未确定的拼音碎片。
- 防抖时间太短（如 300ms）→ 长拼音中间停顿导致碎片漏出；防抖时间太长（如 800ms）→ 汉字上屏后需等待过久才响应，体感卡顿。
- 核心矛盾：防抖只关心"多久没输入"，不关心输入是否处于**合成状态**，因此永远无法从根本上拦截拼音进入业务逻辑。

### 为什么依赖 isComposing 属性需要兜底？

**e.nativeEvent.isComposing** 是现代浏览器（Chrome、Firefox、Safari、Edge 以及国产浏览器极速模式）原生支持的属性，在主流场景下可以直接判断输入是否处于 IME 合成阶段。但它在以下场景不可靠：

- **IE 兼容模式：** 360 安全浏览器、QQ 浏览器、搜狗浏览器等国产双核浏览器在兼容模式下，**InputEvent** 实现可能不完整，**isComposing** 可能缺失或始终返回 false。
- **老旧或定制内核：** 统信 UOS、麒麟 OS 早期版本、微信 XWeb 等部分环境基于旧版 Chromium 或定制 WebKit，对 **InputEvent** 的规范实现存在偏差，**isComposing** 可能无法正确反映合成状态。
- **Polyfill 或事件封装：** 如果项目中存在自定义事件系统或旧版 React 合成事件封装，事件对象可能不是真正的 **InputEvent**，导致该属性不可用。

**结论：** 现代浏览器中优先使用 **isComposing**；但在需要兼容 IE 兼容模式或老旧国产内核时，必须配合 **compositionstart** / **compositionend** 标志位锁作为兜底。

### 为什么"只解锁不补偿"的方案在部分浏览器环境下不可靠？

这个方案看起来和完整实现几乎一样：都有锁，都监听 **compositionstart** / **compositionend** / **input**。唯一的区别是：**它在 compositionend 中只解锁，不主动调用业务逻辑**，而是依赖浏览器在解锁后自动再发一次 **input**。

不同内核的时序差异决定了它的风险：

- **Chromium / Blink / WebKit（Chrome、Safari、Edge、国产浏览器极速模式）：** 典型时序是 **input → compositionend**。用户按下空格或数字键选字时，最后一个 **input** 事件到来时 **isComposing** 还是 **true**，被锁拦截；随后 **compositionend** 触发，锁打开，但浏览器**通常不会再补发一次 input**。导致 **最后一个汉字可能无法触发搜索**。
- **Gecko（Firefox）：** 典型时序是 **compositionend → input**。**compositionend** 先解锁，然后 **input** 正常触发。这个方案在 Firefox 下通常能工作——但这只是统计规律，不是规范保证。
- **老旧内核 / 国产定制环境（统信 UOS、麒麟 OS、微信 XWeb 等）：** 这些环境可能基于老旧 Chromium 或深度定制的 WebKit，对 **composition** 事件的实现存在偏差：有的 **compositionend** 不触发；有的触发时 **e.target.value** 尚未更新；有的触发多次；有的触发后**不补发任何 input**。依赖"解锁后等 input"在这些环境下是不可靠的赌博。

完整实现为什么能覆盖绝大多数主流内核？

- **不赌浏览器的人品：** 在 **compositionend** 中**主动补偿触发一次** **doSearch(e.target.value)**，而不是被动等待浏览器发 **input**。
- **Firefox 双触发去重：** Firefox 下可能触发两次（**compositionend** 中一次 + 后续的 **input** 中一次）。通过 **AbortController** 和请求 ID 进行物理取消 + 逻辑校验，确保只有最后一次生效。
- **Chromium 不丢失：** 最后一个 **input** 被锁拦截，但 **compositionend** 中补偿触发，抹平时序差异。
- **特殊环境兜底：** 只要 **compositionend** 触发（哪怕时序混乱、哪怕只触发一次），业务逻辑就会被执行，不会因为浏览器漏发 **input** 而完全失效。若 **compositionend** 本身不触发，则需要额外兜底策略。

下面的序列图直观地展示了"有补偿"与"无补偿"两种方案在 Chromium 内核下的执行路径差异：

<MermaidViewer source={naiveVsPro} />

<Divider />

### 环境兼容性矩阵：同样的代码，为什么在不同机器上表现不同？

以下四个变量任意组合都会导致 IME 事件时序差异：**Chromium 版本**（Blink 引擎迭代）× **操作系统 IME 管道**（TSF / InputMethodKit / IBus / Fcitx）× **输入法框架**（微软拼音 / 搜狗 / Rime / 微信键盘）× **上屏方式**（空格 / 数字选字）。你的本地环境只是这张巨大矩阵中的一个格子。

<Table dataSource={envDataSource} columns={envColumns} pagination={false} size="small" bordered scroll={{ x: 'max-content' }} />

<Alert
  message="极端情况说明"
  description="对于 compositionend 完全不触发的极端环境（如部分麒麟 OS + Fcitx 组合），任何依赖 composition 事件的方案都需要额外的兜底策略（如监听 beforeinput 事件、使用非受控组件 + 定时检测、或退化到手动触发按钮搜索）。工业级方案的优势在于：它覆盖了 90% 以上的时序混乱场景（时序颠倒、多次触发、value 延迟），而不是解决浏览器引擎本身完全缺失事件的极端缺陷。"
  type="info"
  showIcon
/>

</Card>

<Card
  title={<span>四、 为什么要这样解决 且互动演示 <Tag color="blue">Live Demo</Tag></span>}
  style={{ marginBottom: '24px', border: '2px solid #1890ff' }}
>

现代浏览器首选方案 A：监听 **input** 并判断 **!e.nativeEvent.isComposing**，可直接在内容落地的瞬间执行业务逻辑，无需维护 Ref 状态。Live Demo 为了演示"锁"的物理拦截效果，仍采用方案 B（标志位锁），这样读者可以直观看到拼音碎片如何被阻止进入业务值。

方案 B 的价值在于兼容 IE 兼容模式、老旧国产内核等 **isComposing** 不可靠的环境，同时通过 **compositionend** 中的补偿触发避免 Blink / WebKit 中最后一字丢失的问题。

<LiveDemo />

</Card>

<Card title="五、 Bug 能解决的核心原理" style={{ background: '#f0f5ff' }}>

- **input 与 compositionend 的语义差异：** **input** 表示 DOM 内容已改变，**compositionend** 表示输入法会话关闭。在 Chromium / Blink / 现代 WebKit 中，选词上屏时通常是内容先落地（input），输入法再通知结束（compositionend）。
- **isComposing 原生属性（方案 A）：** 现代浏览器的 **InputEvent** 直接提供 **isComposing** 标志，可以最优雅地区分"合成中"和"已确定"。
- **isComposing 标志位锁（方案 B）：** 通过 **compositionstart** 将锁闭合，在此期间的所有 **input** 事件被拦截；在 **compositionend** 中解锁并主动补偿触发一次业务逻辑，覆盖 **isComposing** 不可靠的环境。
- **时序对齐（保底触发）：** 在 **compositionend** 中手动调用业务逻辑，可以抹平 Chromium / Blink 中 **input** 先于 **compositionend** 导致的"最后一字可能丢失"问题。
- **AbortController 竞态控制：** 结合网络层的取消机制，确保只有最后一次确定的输入（而非中间过程）能生效，解决回包乱序和多次触发问题。

</Card>
